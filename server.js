// server.js removed — this project is frontend-only and talks directly to
// a local Ollama instance. The previous Express proxy has been removed.
// If you still need a local proxy, recreate `server.js` with the desired
// behavior.

export {};
